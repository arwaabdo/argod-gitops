apiVersion: apps/v1
kind: Deployment
metadata:
  name: tvplatform-admin-frontend
  namespace: default
  labels:
    app: tvplatform-admin-frontend
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tvplatform-admin-frontend
      env: prod
  template:
    metadata:
      labels:
        app: tvplatform-admin-frontend
        env: prod
    spec:
      containers:
      - name: tvplatform-admin-frontend
        image: registry.digitalocean.com/obs-tvplatform-backend/tvplatform-admin-frontend:3-prod # will be updated by jenkins PRs
        imagePullPolicy: Always
        envFrom:
        - secretRef:
            name: tvplatform-admin-frontend-prod
      imagePullSecrets:
      - name: obs-tvplatform-backend
