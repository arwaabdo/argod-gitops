apiVersion: apps/v1
kind: Deployment
metadata:
  name: tvplatform-admin-authentication
  namespace: default
  labels:
    app: tvplatform-admin-authentication
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tvplatform-admin-authentication
      env: prod
  template:
    metadata:
      labels:
        app: tvplatform-admin-authentication
        env: prod
    spec:
      containers:
      - name: tvplatform-admin-authentication
        image: registry.digitalocean.com/obs-tvplatform-backend/tvplatform-admin-authentication:8-prod # will be updated by jenkins PRs
        imagePullPolicy: Always
        envFrom:
        - secretRef:
            name: tvplatform-admin-authentication-prod
      imagePullSecrets:
      - name: obs-tvplatform-backend
