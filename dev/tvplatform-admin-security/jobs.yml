apiVersion: batch/v1
kind: Job
metadata:
  name: tvplatform-admin-security-dbmigration
  annotations:
    argocd.argoproj.io/hook: PreSync  # Run this Job before any other resource
spec:
  ttlSecondsAfterFinished: 120
  template:
    spec:
      containers:
      - name: migration
        image: python:3.11-slim  # Replace with your desired Python version
        command: ["/bin/sh", "-c"]
        args:
          - |
            apt-get update && apt-get install -y git &&
            git clone https://${GITHUB_TOKEN}@github.com/OceanBlueSoftware/tvplatform-admin-security.git /app &&
            cd /app &&
            pip3 install poetry &&
            poetry install &&
            poetry run mypy . &&
            poetry run alembic upgrade head
        env:
        - name: GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: github-token-secret
              key: GITHUB_TOKEN
        envFrom:
        - secretRef:
            name: tvplatform-admin-security-dev  # Mount environment variables from the secret
      restartPolicy: Never  # Specify the restart policy
