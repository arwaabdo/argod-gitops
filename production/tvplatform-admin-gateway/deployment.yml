apiVersion: apps/v1
kind: Deployment
metadata:
  name: tvplatform-admin-gateway-prod
  namespace: default
  labels:
    app: tvplatform-admin-gateway-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tvplatform-admin-gateway-prod
  template:
    metadata:
      labels:
        app: tvplatform-admin-gateway-prod
    spec:
      containers:
      - name: tvplatform-admin-gateway-prod
        image: registry.digitalocean.com/obs-tvplatform-backend/tvplatform-admin-gateway:24 # will be updated by jenkins PRs
        imagePullPolicy: Always
      imagePullSecrets:
      - name: obs-tvplatform-backend
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: env
                operator: In
                values:
                - production
      tolerations:
      - key: "env"
        operator: "Equal"
        value: "production"
        effect: "NoSchedule"
