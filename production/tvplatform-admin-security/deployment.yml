apiVersion: apps/v1
kind: Deployment
metadata:
  name: tvplatform-admin-security-prod
  namespace: default
  labels:
    app: tvplatform-admin-security-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tvplatform-admin-security-prod
  template:
    metadata:
      labels:
        app: tvplatform-admin-security-prod
    spec:
      containers:
      - name: tvplatform-admin-security-prod
        image: registry.digitalocean.com/obs-tvplatform-backend/tvplatform-admin-security:3 # will be updated by jenkins PRs
        imagePullPolicy: Always
      imagePullSecrets:
      - name: obs-tvplatform-backend
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: env
                operator: In
                values:
                - production
      tolerations:
      - key: "env"
        operator: "Equal"
        value: "production"
        effect: "NoSchedule"
