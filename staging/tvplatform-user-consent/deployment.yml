apiVersion: apps/v1
kind: Deployment
metadata:
  name: tvplatform-user-consent
  namespace: default
  labels:
    app: tvplatform-user-consent
    env: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tvplatform-user-consent
      env: staging
  template:
    metadata:
      labels:
        app: tvplatform-user-consent
        env: staging
    spec:
      containers:
      - name: tvplatform-user-consent
        image: registry.digitalocean.com/obs-tvplatform-backend/tvplatform-user-consent:2-staging # will be updated by jenkins PRs
        imagePullPolicy: Always
        envFrom:
        - secretRef:
            name: tvplatform-admin-authentication-staging
      imagePullSecrets:
      - name: obs-tvplatform-backend