apiVersion: apps/v1
kind: Deployment
metadata:
  name: tvplatform-admin-security-staging
  namespace: default
  labels:
    app: tvplatform-admin-security-staging
    env: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tvplatform-admin-security-staging
      env: staging
  template:
    metadata:
      labels:
        app: tvplatform-admin-security-staging
        env: staging
    spec:
      containers:
      - name: tvplatform-admin-security-staging
        image: registry.digitalocean.com/obs-tvplatform-backend/tvplatform-admin-security:3 # will be updated by jenkins PRs
        imagePullPolicy: Always
      imagePullSecrets:
      - name: obs-tvplatform-backend
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            preference:
              matchExpressions:
              - key: env
                operator: In
                values:
                - staging
      tolerations:
      - key: "env"
        operator: "Equal"
        value: "staging"
        effect: "PreferNoSchedule"
