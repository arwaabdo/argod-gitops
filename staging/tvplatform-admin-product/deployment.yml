apiVersion: apps/v1
kind: Deployment
metadata:
  name: tvplatform-admin-product-staging
  namespace: default
  labels:
    app: tvplatform-admin-product-staging
    env: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tvplatform-admin-product-staging
      env: staging
  template:
    metadata:
      labels:
        app: tvplatform-admin-product-staging
        env: staging
    spec:
      containers:
      - name: tvplatform-admin-product-staging
        image: registry.digitalocean.com/obs-tvplatform-backend/tvplatform-admin-product:7 # will be updated by jenkins PRs
        imagePullPolicy: Always
      imagePullSecrets:
      - name: obs-tvplatform-backend
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            preference:
              matchExpressions:
              - key: env
                operator: In
                values:
                - staging
      tolerations:
      - key: "env"
        operator: "Equal"
        value: "production"
        effect: "NoSchedule"